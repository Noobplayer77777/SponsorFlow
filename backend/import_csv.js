const fs = require('fs'); const { parse } = require('csv-parse/sync'); const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const data = fs.readFileSync('../sponsors_ready.csv', 'utf8'); const records = parse(data, { columns: true }); const valid = records.filter(r => r.companyName); console.log('Importing', valid.length, 'records...'); await prisma.company.createMany({ data: valid.map(r => ({ companyName: r.companyName, email: r.email || null, phoneNumber: r.phoneNumber || null, contactPerson: r.contactPerson || null })) }); console.log('Done!'); } main().then(() => prisma.$disconnect());
